# Core dependencies
requests>=2.28
beautifulsoup4>=4.11
lxml>=4.9
pandas>=1.5
numpy>=1.23

# Optional but recommended — scripts degrade gracefully without them
tldextract>=3.4        # correct registered-domain parsing for multi-part TLDs (.ac.uk, .edu.pk)
python-whois>=0.8      # domain age / registrant privacy features
